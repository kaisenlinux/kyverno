Author: Kevin Chevreuil - Kaisen <kaisen@kaisenlinux.org>

--- kyverno-1.12.1.orig/Makefile
+++ kyverno-1.12.1/Makefile
@@ -165,7 +165,7 @@ BACKGROUND_BIN := $(BACKGROUND_DIR)/back
 PACKAGE        ?= github.com/kyverno/kyverno
 CGO_ENABLED    ?= 0
 ifdef VERSION
-LD_FLAGS       := "-s -w -X $(PACKAGE)/pkg/version.BuildVersion=$(VERSION)"
+LD_FLAGS       := "-s -w -X $(PACKAGE)/pkg/version.BuildVersion=1.12.1"
 else
 LD_FLAGS       := "-s -w"
 endif
